/target
*.log
# Per-arch target dirs used by build_linux.sh's Docker cross-compile (normally
# removed by the script itself, but left behind if it's interrupted)
/target-docker-*

# macOS
.DS_Store

# Windows
Thumbs.db
Desktop.ini

# Editors / IDEs
.vscode/
.idea/
*.iml
*.swp
*.swo
*~

# Local secrets / environment overrides
.env
.env.*

# Build/dist output
/dist
/dist_mac
/dist_linux
/KinetixInstaller.exe

# Transient debug/diagnostic dumps
/errors.json
/errors.txt
/debug_output.txt
/opt_metrics.txt
/tests_error.txt
/lsp_errors.txt
/test_output.txt
